# Copy to `.env` in this directory and set a real token before starting the stack:
#   cp .env.example .env
# The value must be at least 8 characters (AppConfig validates at startup).
# `.env` is gitignored — never commit the real token.
TELCO_API_BEARER_TOKEN=replace-me
